#include <stdio.h>

typedef int antelope;

typedef int antelope, bagel, mushroom;

int main() {
    antelope x = 10;
    bagel y = 20;
    mushroom z = 30;

    printf("x = %d, y = %d, z = %d\n", x, y, z);
    return 0;
}
